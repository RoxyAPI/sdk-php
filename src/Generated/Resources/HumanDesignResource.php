<?php

declare(strict_types=1);

/**
 * AUTO-GENERATED by scripts/generate.mjs from the live OpenAPI spec.
 * Do not edit this file by hand. Regenerate with: node scripts/generate.mjs
 */

namespace RoxyAPI\Sdk\Generated\Resources;

use RoxyAPI\Sdk\Generated\Resources\BaseResource;

/**
 * Generate the full Human Design bodygraph from a birth moment: type, strategy, inner
 * authority, profile, definition, i...
 *
 * Accessed via $roxy->humanDesign.
 */
class HumanDesignResource extends BaseResource
{
    /**
     * Calculate the nine Human Design centers
     *
     * Calculate the state of all nine Human Design centers for a birth moment: whether each is
     * defined or open, whether it is a motor or an awareness center, its theme, and the active
     * gates it holds. The data layer behind a rendered bodygraph where defined centers are colored
     * and open centers are white.
     *
     * POST /human-design/centers
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateCenters(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateCentersRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate the defined Human Design channels
     *
     * Calculate the defined channels for a birth moment. A channel is defined when both of its
     * gates are activated, and it wires together the two centers it connects. Returns each defined
     * channel with its gates, name, circuit family, and connected centers, plus the full set of
     * centers those channels define. Built for bodygraph rendering and definition analysis.
     *
     * POST /human-design/channels
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateChannels(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateChannelsRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate Human Design connection chart - Two-person composite bodygraph compatibility
     *
     * Calculate a Human Design connection chart by overlaying two bodygraphs. For each of the 36
     * channels the dynamic between the two people is classified as electromagnetic, dominance,
     * compromise, or companionship, the four mechanics of how two designs meet. Also returns the
     * nine centers as defined or open in the combined bodygraph with which person defines each,
     * the combined definition, and a count of each dynamic. Built for relationship, dating, and
     * coaching tools.
     *
     * POST /human-design/connection
     *
     * @param array $personA
     *   Birth moment of the first person in the connection.
     * @param array $personB
     *   Birth moment of the second person in the connection.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateConnection(
        array $personA,
        array $personB,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateConnectionRequest(personA: $personA, personB: $personB, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate the 26 Human Design gate activations
     *
     * Calculate the 26 gate activations for a birth moment, split into the 13 conscious
     * Personality activations at birth and the 13 unconscious Design activations 88 degrees of
     * solar arc earlier. Each activation reports the planet, gate, line, gate keynote, and the
     * matching I-Ching hexagram. Built for activation columns and detailed chart views.
     *
     * POST /human-design/gates
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateGates(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateGatesRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate Human Design Penta - Small-group BG5 operating system for three to five people
     *
     * Calculate the Human Design Penta (BG5, Base Group 5) for a small group of three to five
     * people. The Penta is a trans-auric form built from a fixed set of six channels running only
     * between the Sacral, the G Center, and the Throat. It reports which of the twelve Penta gates
     * are filled and by whom, which of the six channels are defined Strengths, the upper
     * leadership channels versus the lower generative channels, the 2/14 material core, and the
     * functional gaps where no member supplies a role. Built for team, family, and group analysis
     * tools. Below three people no Penta forms and above five a second Penta emerges, so the group
     * size must be three to five.
     *
     * POST /human-design/penta
     *
     * @param array $members
     *   Birth moments of the three to five people in the group. Below three no Penta forms; above
     *   five a second Penta emerges.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculatePenta(
        array $members,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculatePentaRequest(members: $members, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate the Human Design profile and line keynotes
     *
     * Calculate the Human Design profile for a birth moment: the conscious Personality Sun line
     * over the unconscious Design Sun line, with the keynote for each. The profile is the geometry
     * of the life role, for example 5/1 the Heretic Investigator. Verified against NASA JPL
     * Horizons positions.
     *
     * POST /human-design/profile
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateProfile(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateProfileRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate Human Design type, authority and profile
     *
     * Calculate the core Human Design identity from a birth moment: the energy type, the aura
     * strategy, the inner authority, the signature and not-self themes, and the profile. The fast
     * lookup for type-and-authority features without the full bodygraph payload. Verified against
     * NASA JPL Horizons positions.
     *
     * POST /human-design/type
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateType(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateTypeRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Calculate Human Design Variables - The four arrows and Color, Tone, Base substructure
     *
     * Calculate the four Human Design Variable arrows for a birth moment: Determination and
     * Environment on the design side, Perspective and Motivation on the personality side. Each
     * arrow returns its Color, Tone, and Base numbers from the hexagram-line substructure, the
     * left or right direction set by the Tone, and the sourced Color and direction labels. This is
     * the advanced Rave Variables and Primary Health System layer beneath Type, Strategy,
     * Authority, and Profile. Color, Tone, and Base shift with tiny differences in birth time, so
     * each arrow carries a confidence flag that turns false near a Color or Tone boundary, and a
     * precise birth time is essential. Built for Human Design apps offering PHS, diet,
     * environment, and Rave Psychology readings.
     *
     * POST /human-design/variables
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function calculateVariables(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\CalculateVariablesRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Generate full Human Design bodygraph - Type, authority, profile, centers, channels, gates
     *
     * Generate a complete Human Design bodygraph from a birth date, time, and timezone. Returns
     * the energy type, strategy, inner authority, signature, not-self theme, profile, definition,
     * incarnation cross, all nine centers with defined state and active gates, the defined
     * channels, and all 26 planetary activations across the Personality and Design sides. The
     * single endpoint for a full chart in one call, built for Human Design apps, readings, and
     * coaching tools.
     *
     * POST /human-design/bodygraph
     *
     * @param string $date
     *   Birth date in YYYY-MM-DD format. The anchor for both the Personality activations at birth
     *   and the Design activations 88 degrees of solar arc earlier.
     * @param string $time
     *   Birth time in 24-hour HH:MM:SS format. Precision matters: the profile lines and gate
     *   boundaries shift with the exact minute of birth.
     * @param mixed $timezone
     *   IANA name (e.g. "America/New_York", "Europe/London", "UTC"), decimal hours (e.g. -5 for EST,
     *   1 for CET), or a fixed UTC offset (e.g. "-05:00", "+01:00"). Prefer the IANA name: it is
     *   resolved to the DST-correct offset for the birth date, while a fixed offset or decimal is
     *   taken literally and will be wrong if it does not match the daylight-saving state on that
     *   date. Invalid timezones return 400 with a validation error.
     * @param float|null $latitude
     *   Birth latitude in decimal degrees. Optional and does not affect the bodygraph, which depends
     *   only on ecliptic longitudes. Defaults to 0.
     * @param float|null $longitude
     *   Birth longitude in decimal degrees. Optional and does not affect the bodygraph. Defaults to
     *   0.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function generateBodygraph(
        string $date,
        string $time,
        mixed $timezone,
        ?float $latitude = null,
        ?float $longitude = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\GenerateBodygraphRequest(date: $date, time: $time, timezone: $timezone, latitude: $latitude, longitude: $longitude, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Generate Human Design transit overlay - Current planetary activations on a natal bodygraph
     *
     * Overlay the current or any given planetary positions on a natal Human Design bodygraph to
     * see which channels the transit temporarily completes. Returns the 13 transiting body
     * activations with gate and line, the channels the transit completes beyond the natal
     * definition split into personal channels where the transit supplies the partner gate of a
     * natal gate and educational channels where the transit supplies both gates, the natally open
     * centers those channels temporarily define, and a short factual summary. A transit is a
     * single moment, so there is no Design side. When date and time are omitted the overlay is
     * computed for now in UTC. Built for daily Human Design apps, transit widgets, and
     * notification tools.
     *
     * POST /human-design/transit
     *
     * @param array $birthData
     *   Birth moment whose natal bodygraph the transit is overlaid on.
     * @param string|null $date
     *   Transit date in YYYY-MM-DD UTC. Optional. Defaults to today in UTC when omitted, giving the
     *   just-now transit.
     * @param string|null $time
     *   Transit time in HH:MM:SS UTC. Optional. Defaults to the current UTC time when omitted.
     *   Precision matters: the Moon moves through a gate in roughly half a day.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function generateTransit(
        array $birthData,
        ?string $date = null,
        ?string $time = null,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\GenerateTransitRequest(birthData: $birthData, date: $date, time: $time, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Look up a Human Design center by id
     *
     * Look up the static reference data for one of the nine Human Design centers by its id: the
     * display name, whether it is a motor or awareness center, and what it means both defined and
     * undefined. A pure reference endpoint with no birth data required.
     *
     * GET /human-design/centers/{id}
     *
     * @param string $id
     *   Center id. One of head, ajna, throat, g, heart, sacral, solar-plexus, spleen, root.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function getCenter(
        string $id,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\GetCenterRequest(id: $id, lang: $lang);

        return $this->callRequest($request);
    }

    /**
     * Look up a Human Design gate by number
     *
     * Look up the static reference data for a Human Design gate by its number from 1 to 64: the
     * gate keynote name, the center it sits in, the matching I-Ching hexagram, and the gates that
     * form a channel with it. A pure reference endpoint with no birth data required.
     *
     * GET /human-design/gates/{number}
     *
     * @param int $number
     *   Gate number from 1 to 64.
     * @param string|null $lang
     *   Response language (ISO 639-1). Supported: en, tr, de, es, hi, pt, fr, ru. Defaults to en.
     *   Languages without translations yet return English.
     *
     * @return array<string, mixed>
     */
    public function getGate(
        int $number,
        ?string $lang = null
    ): array
    {
        $request = new \RoxyAPI\Sdk\Generated\Requests\GetGateRequest(number: $number, lang: $lang);

        return $this->callRequest($request);
    }
}
